package ride.customer.domain.entities._enum

enum class RideStatus {
    WAITING,
    DIRECT,
    SCHEDULED,
    ASSIGNED,
    ACCEPTED,
    ON_HOLD,
    READY,
    ON_GOING,
    FINISHED,
    CANCELLED,
    REJECTED,
    TIMEOUT
}